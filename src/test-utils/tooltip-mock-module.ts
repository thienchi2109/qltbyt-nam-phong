import {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from "./tooltip-mock"

/** Provides a module-shaped tooltip mock for vi.mock factories. */
export const tooltipMockModule = {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
}
